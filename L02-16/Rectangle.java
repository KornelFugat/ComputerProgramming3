
public class Rectangle{
    int height;
    int length;
    
    public void display(){
        System.out.println("dimensions: " + height + "x" + length +"\nperimeters: " + 2*length+2*height + "\nsurface: " + height*length);
    }
}
